## What changed?

<!-- File(s) and what the change does. One sentence per file is fine. -->

## Why?

<!-- For numbers: cite the source and its date. For method changes (gates, modes, rubric): link the issue where it was discussed. -->

## Checks

- [ ] `python tools/validate_skill.py` passes
- [ ] `python -m unittest test_botec` passes
- [ ] SKILL.md stays under 500 lines; references stay one level deep
- [ ] No new external dependencies; botec.py remains stdlib-only
- [ ] Time-sensitive facts labeled "approximate — verify"
